package tarski
package model

/** Enumeration for the remaining buttons: to evaluate formulas, to add / move / delete a block, and the icon display of
  * currently selected block.
  */
enum Action:
  case Eval, Add, Del, Move, Icon
